require 'spec_helper'

describe FileutilsLogger do
  it 'has a version number' do
    expect(FileutilsLogger::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
